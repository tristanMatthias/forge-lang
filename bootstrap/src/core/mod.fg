// WHY: aggregator for the stable infrastructure that every phase
// depends on (token, ast, scanner state, diagnostic, resolver, cg).
// As the migration progresses, more modules join this list.

mod token
mod ast
mod scanner
mod resolver
mod eval
mod cg
