let names = ["alice", "bob", "carol"]
for name in names {
    println(name)
}

// For-in with mutation
let nums = [1, 2, 3, 4, 5]
mut sum = 0
for n in nums {
    sum = sum + n
}
println(string(sum))

// For-in with break
for x in [10, 20, 30, 40] {
    if x == 30 { break }
    println(string(x))
}
