fn main() -> int {
    // Basic for loop
    for i in 0..5 {
        println(string(i))
    }

    // For loop with computed bounds
    let n = 3
    mut sum = 0
    for j in 1..n + 1 {
        sum = sum + j
    }
    println(string(sum))

    0
}
