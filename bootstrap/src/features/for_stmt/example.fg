fn main() -> int {
    // Basic for loop
    for i in 0..5 {
        i |> string |> println
    }

    // For loop with computed bounds
    let n = 3
    mut sum = 0
    for j in 1..n + 1 {
        sum = sum + j
    }
    sum |> string |> println

    0
}
