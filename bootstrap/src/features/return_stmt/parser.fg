// WHY: parses the `return` statement, both with and without a value.

use core.ast.{Stmt}
use parse.{Parser}
use core.ast.{Tk}

impl Parser {
    fn parse_return_statement(self) -> Stmt? {
        self.advance()

        if self.check(Tk.RightBrace) || self.check(Tk.Eof) {
            return Stmt.ReturnEmpty
        }

        let value = self.parse_expression()?
        Stmt.Return(value)
    }
}
