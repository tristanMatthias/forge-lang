mod parser
mod codegen

fn kw_parse_enum(p: Parser) -> Stmt? {
    p.parse_enum_declaration()
}

export fn init_enum_decl(reg: Registry) {
    let f = feature_new("enum_decl") with {
        decl_keyword: "enum",
        parse_decl: kw_parse_enum,
    }
    register(reg, f)
}
