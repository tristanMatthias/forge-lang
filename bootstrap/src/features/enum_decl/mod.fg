mod parser
mod codegen
use core.registry.{feature_new, register}

export fn init_enum_decl(reg: Registry) {
    let f = feature_new("enum_decl") with {
        decl_keyword: "kw_enum",
        parse_decl: (p: Parser) -> p.parse_enum_declaration(),
    }
    register(reg, f)
}
