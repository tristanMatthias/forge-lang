mod parser
mod codegen
use core.registry.{feature_new, register}

export fn init_fn_decl(reg: Registry) {
    let f = feature_new("fn_decl") with {
        decl_keyword: "kw_fn",
        parse_decl: (p: Parser) -> p.parse_function_declaration(),
    }
    register(reg, f)
}
