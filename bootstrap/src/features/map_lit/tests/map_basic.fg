let m = {"name": "Alice", "age": "30"}
println(m.get("name"))
println(m.get("age"))
println(string(m.has("name")))
println(string(m.has("missing")))
m.length |> string |> println

m.set("email", "alice@test.com")
println(m.get("email"))
m.length |> string |> println

let keys = m.keys()
keys.length |> string |> println

let empty = {}
empty.length |> string |> println
