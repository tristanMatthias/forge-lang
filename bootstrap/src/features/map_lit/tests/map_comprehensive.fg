// Basic map operations
let m = {"x": "10", "y": "20", "z": "30"}
m.length |> string |> println
println(m.get("x"))
println(m.get("z"))

// has
println(string(m.has("x")))
println(string(m.has("w")))

// set overwrites
m.set("x", "99")
println(m.get("x"))

// Empty map
let e = {}
e.length |> string |> println
println(string(e.has("anything")))

// keys returns a list
let k = m.keys()
k.length |> string |> println

// Map with numeric values (stored as i64)
let nums = {"a": 1, "b": 2, "c": 3}
let sum = nums.get("a") + nums.get("b") + nums.get("c")
sum |> string |> println
