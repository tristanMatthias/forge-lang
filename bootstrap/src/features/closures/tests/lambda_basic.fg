// Basic lambda
let double = (x) -> x * 2
println(string(double(5)))

// Lambda with multiple params
let add = (a, b) -> a + b
println(string(add(3, 4)))

// List.map with lambda
let nums = [1, 2, 3, 4, 5]
let doubled = nums.map((x) -> x * 2)
doubled[0] |> string |> println
doubled[4] |> string |> println

// List.filter — check if even (x % 2 == 0 approximated)
let big = nums.filter((x) -> x > 3)
big.length |> string |> println
big[0] |> string |> println

// List.reduce
let sum = nums.reduce(0, (acc, x) -> acc + x)
sum |> string |> println

// Zero-arg lambda
let greet = () -> 42
println(string(greet()))
