mod parser
use core.registry.{feature_new, register}

export fn init_extern_decl(reg: Registry) {
    let f = feature_new("extern_decl") with {
        decl_keyword: "kw_extern",
        parse_decl: (p: Parser) -> p.parse_extern_declaration(),
    }
    register(reg, f)
}
