mod codegen

export fn register_list_lit(reg: Registry) -> Registry {
    register_feature(reg, Feature { name: "list_lit", expr_tag: 25, stmt_tag: 0 - 1, emit_expr: list_emit, emit_stmt: noop_stmt })
}

fn list_emit(ctx: Ctx, env: VarEnv, expr: Expr) -> EmitResult {
    match expr { .ListLit(elements) -> emit_list_lit(ctx, env, elements), _ -> err_emit("expected ListLit") }
}
