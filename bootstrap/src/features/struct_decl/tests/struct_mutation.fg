type Point = {
    x: int,
    y: int,
}

impl Point {
    fn move_right(self, dx: int) {
        self.x = self.x + dx
    }
}

fn main() {
    mut p = Point { x: 1, y: 2 }
    p.x |> string |> println
    p.move_right(10)
    p.x |> string |> println
    p.y = 99
    p.y |> string |> println
}
