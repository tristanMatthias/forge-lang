// WHY: generic type parameters (`<T>`) on functions, types, and enums.
// In the bootstrap's everything-is-i64 model, generics are erased at
// parse time. The parsing lives in the declaration parsers (fn_decl,
// struct_decl, enum_decl, impl_decl) via skip_angle_brackets().
// This module exists for feature directory completeness.
