// Force unwrap operator (currently no-op in bootstrap)
let x = 42
let y = x!
y |> string |> println

// Force unwrap on a non-null value
let s = "hello"
let s2 = s!
println(s2)

// Chained force unwrap
let z = 99!!!!
z |> string |> println
