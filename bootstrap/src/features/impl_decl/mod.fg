mod parser

fn kw_parse_impl(p: Parser) -> Stmt? {
    p.parse_impl_declaration()
}

export fn init_impl_decl(reg: Registry) {
    let f = feature_new("impl_decl") with {
        decl_keyword: "kw_impl",
        parse_decl: kw_parse_impl,
    }
    register(reg, f)
}
