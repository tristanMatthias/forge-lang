// GENERATED by `bash scripts/diagnose.sh --emit-regen-kw` — DO NOT EDIT BY HAND.
// Source of truth: keyword_rules() in parse/token_table.av (compiled to an
// exact-match byte-branch trie by parse/emit_lex.av). Editing the keyword table
// without regenerating fails parse/tests/gen_keyword_scanner_regen_test.av.
use parse.{byte_at, LOWER_A, LOWER_B, LOWER_C, LOWER_D, LOWER_E, LOWER_F, LOWER_G, LOWER_H, LOWER_I, LOWER_K, LOWER_L, LOWER_M, LOWER_N, LOWER_O, LOWER_P, LOWER_Q, LOWER_R, LOWER_S, LOWER_T, LOWER_U, LOWER_V, LOWER_W, LOWER_X, LOWER_Y}
use core.{Tk}

/// GENERATED exact-match keyword recogniser for the keyword table.
/// Returns the keyword `Tk` for the identifier bytes [start, end), or
/// `Tk.Identifier` when they spell no keyword.
export fn classify_keyword(src: bytes, start: int, end: int) -> Tk {
    let b0 = byte_at(src, start)
    if b0 == LOWER_A {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_N {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_D {
                if start + 3 == end { return Tk.KwAnd }
            }
        }
        if b1 == LOWER_S {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_Y {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_N {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_C {
                        if start + 5 == end { return Tk.KwReserved }
                    }
                }
            }
        }
        if b1 == LOWER_W {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_A {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_I {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_T {
                        if start + 5 == end { return Tk.KwReserved }
                    }
                }
            }
        }
    }
    if b0 == LOWER_B {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_A {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_R {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_E {
                    if start + 4 == end { return Tk.KwReserved }
                }
            }
        }
        if b1 == LOWER_O {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_R {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_R {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_O {
                        let b5 = byte_at(src, start + 5)
                        if b5 == LOWER_W {
                            if start + 6 == end { return Tk.KwReserved }
                        }
                    }
                }
            }
        }
        if b1 == LOWER_R {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_E {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_A {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_K {
                        if start + 5 == end { return Tk.KwBreak }
                    }
                }
            }
        }
    }
    if b0 == LOWER_C {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_A {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_T {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_C {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_H {
                        if start + 5 == end { return Tk.KwCatch }
                    }
                }
            }
        }
        if b1 == LOWER_H {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_A {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_N {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_N {
                        let b5 = byte_at(src, start + 5)
                        if b5 == LOWER_E {
                            let b6 = byte_at(src, start + 6)
                            if b6 == LOWER_L {
                                if start + 7 == end { return Tk.KwChannel }
                            }
                        }
                    }
                }
            }
        }
        if b1 == LOWER_O {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_M {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_P {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_O {
                        let b5 = byte_at(src, start + 5)
                        if b5 == LOWER_N {
                            let b6 = byte_at(src, start + 6)
                            if b6 == LOWER_E {
                                let b7 = byte_at(src, start + 7)
                                if b7 == LOWER_N {
                                    let b8 = byte_at(src, start + 8)
                                    if b8 == LOWER_T {
                                        if start + 9 == end { return Tk.KwComponent }
                                    }
                                }
                            }
                        }
                    }
                }
            }
            if b2 == LOWER_N {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_S {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_T {
                        if start + 5 == end { return Tk.KwConst }
                    }
                }
                if b3 == LOWER_T {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_I {
                        let b5 = byte_at(src, start + 5)
                        if b5 == LOWER_N {
                            let b6 = byte_at(src, start + 6)
                            if b6 == LOWER_U {
                                let b7 = byte_at(src, start + 7)
                                if b7 == LOWER_E {
                                    if start + 8 == end { return Tk.KwContinue }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
    if b0 == LOWER_D {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_E {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_F {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_E {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_R {
                        if start + 5 == end { return Tk.KwDefer }
                    }
                }
            }
        }
        if b1 == LOWER_Y {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_N {
                if start + 3 == end { return Tk.KwDyn }
            }
        }
    }
    if b0 == LOWER_E {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_L {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_S {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_E {
                    if start + 4 == end { return Tk.KwElse }
                }
            }
        }
        if b1 == LOWER_N {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_U {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_M {
                    if start + 4 == end { return Tk.KwEnum }
                }
            }
        }
        if b1 == LOWER_R {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_R {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_D {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_E {
                        let b5 = byte_at(src, start + 5)
                        if b5 == LOWER_F {
                            let b6 = byte_at(src, start + 6)
                            if b6 == LOWER_E {
                                let b7 = byte_at(src, start + 7)
                                if b7 == LOWER_R {
                                    if start + 8 == end { return Tk.KwErrdefer }
                                }
                            }
                        }
                    }
                }
            }
        }
        if b1 == LOWER_X {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_P {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_O {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_R {
                        let b5 = byte_at(src, start + 5)
                        if b5 == LOWER_T {
                            if start + 6 == end { return Tk.KwExport }
                        }
                    }
                }
            }
            if b2 == LOWER_T {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_E {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_R {
                        let b5 = byte_at(src, start + 5)
                        if b5 == LOWER_N {
                            if start + 6 == end { return Tk.KwExtern }
                        }
                    }
                }
            }
        }
    }
    if b0 == LOWER_F {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_A {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_L {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_S {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_E {
                        if start + 5 == end { return Tk.KwFalse }
                    }
                }
            }
        }
        if b1 == LOWER_N {
            if start + 2 == end { return Tk.KwFn }
        }
        if b1 == LOWER_O {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_R {
                if start + 3 == end { return Tk.KwFor }
            }
        }
    }
    if b0 == LOWER_G {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_I {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_V {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_E {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_N {
                        if start + 5 == end { return Tk.KwGiven }
                    }
                }
            }
        }
    }
    if b0 == LOWER_H {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_A {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_R {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_D {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_W {
                        let b5 = byte_at(src, start + 5)
                        if b5 == LOWER_A {
                            let b6 = byte_at(src, start + 6)
                            if b6 == LOWER_R {
                                let b7 = byte_at(src, start + 7)
                                if b7 == LOWER_E {
                                    if start + 8 == end { return Tk.KwReserved }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
    if b0 == LOWER_I {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_F {
            if start + 2 == end { return Tk.KwIf }
        }
        if b1 == LOWER_M {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_P {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_L {
                    if start + 4 == end { return Tk.KwImpl }
                }
            }
        }
        if b1 == LOWER_N {
            if start + 2 == end { return Tk.KwIn }
        }
        if b1 == LOWER_S {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_O {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_L {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_A {
                        let b5 = byte_at(src, start + 5)
                        if b5 == LOWER_T {
                            let b6 = byte_at(src, start + 6)
                            if b6 == LOWER_E {
                                let b7 = byte_at(src, start + 7)
                                if b7 == LOWER_D {
                                    if start + 8 == end { return Tk.KwIsolated }
                                }
                            }
                        }
                    }
                }
            }
            if start + 2 == end { return Tk.KwIs }
        }
    }
    if b0 == LOWER_L {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_E {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_T {
                if start + 3 == end { return Tk.KwLet }
            }
        }
    }
    if b0 == LOWER_M {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_A {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_C {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_R {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_O {
                        if start + 5 == end { return Tk.KwReserved }
                    }
                }
            }
            if b2 == LOWER_T {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_C {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_H {
                        if start + 5 == end { return Tk.KwMatch }
                    }
                }
            }
        }
        if b1 == LOWER_O {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_D {
                if start + 3 == end { return Tk.KwMod }
            }
            if b2 == LOWER_V {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_E {
                    if start + 4 == end { return Tk.KwReserved }
                }
            }
        }
        if b1 == LOWER_U {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_T {
                if start + 3 == end { return Tk.KwMut }
            }
        }
    }
    if b0 == LOWER_N {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_O {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_N {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_E {
                    if start + 4 == end { return Tk.KwNone }
                }
            }
            if b2 == LOWER_T {
                if start + 3 == end { return Tk.KwNot }
            }
        }
        if b1 == LOWER_U {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_L {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_L {
                    if start + 4 == end { return Tk.KwNull }
                }
            }
        }
    }
    if b0 == LOWER_O {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_R {
            if start + 2 == end { return Tk.KwOr }
        }
        if b1 == LOWER_W {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_N {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_E {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_D {
                        if start + 5 == end { return Tk.KwReserved }
                    }
                }
            }
        }
    }
    if b0 == LOWER_P {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_A {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_R {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_A {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_L {
                        let b5 = byte_at(src, start + 5)
                        if b5 == LOWER_L {
                            let b6 = byte_at(src, start + 6)
                            if b6 == LOWER_E {
                                let b7 = byte_at(src, start + 7)
                                if b7 == LOWER_L {
                                    if start + 8 == end { return Tk.KwParallel }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
    if b0 == LOWER_Q {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_U {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_O {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_T {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_E {
                        if start + 5 == end { return Tk.KwQuote }
                    }
                }
            }
        }
    }
    if b0 == LOWER_R {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_E {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_F {
                if start + 3 == end { return Tk.KwReserved }
            }
            if b2 == LOWER_T {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_U {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_R {
                        let b5 = byte_at(src, start + 5)
                        if b5 == LOWER_N {
                            if start + 6 == end { return Tk.KwReturn }
                        }
                    }
                }
            }
        }
    }
    if b0 == LOWER_S {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_E {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_L {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_E {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_C {
                        let b5 = byte_at(src, start + 5)
                        if b5 == LOWER_T {
                            if start + 6 == end { return Tk.KwSelect }
                        }
                    }
                }
            }
        }
        if b1 == LOWER_H {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_A {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_P {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_E {
                        if start + 5 == end { return Tk.KwShape }
                    }
                }
            }
        }
        if b1 == LOWER_P {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_A {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_W {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_N {
                        if start + 5 == end { return Tk.KwSpawn }
                    }
                }
            }
            if b2 == LOWER_E {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_C {
                    if start + 4 == end { return Tk.KwSpec }
                }
            }
        }
        if b1 == LOWER_Y {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_S {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_T {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_E {
                        let b5 = byte_at(src, start + 5)
                        if b5 == LOWER_M {
                            let b6 = byte_at(src, start + 6)
                            if b6 == LOWER_S {
                                if start + 7 == end { return Tk.KwReserved }
                            }
                        }
                    }
                }
            }
        }
    }
    if b0 == LOWER_T {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_H {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_E {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_N {
                    if start + 4 == end { return Tk.KwThen }
                }
            }
        }
        if b1 == LOWER_R {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_A {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_I {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_T {
                        if start + 5 == end { return Tk.KwTrait }
                    }
                }
            }
            if b2 == LOWER_U {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_E {
                    if start + 4 == end { return Tk.KwTrue }
                }
            }
        }
        if b1 == LOWER_Y {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_P {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_E {
                    if start + 4 == end { return Tk.KwType }
                }
            }
        }
    }
    if b0 == LOWER_U {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_N {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_S {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_A {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_F {
                        let b5 = byte_at(src, start + 5)
                        if b5 == LOWER_E {
                            if start + 6 == end { return Tk.KwReserved }
                        }
                    }
                }
            }
        }
        if b1 == LOWER_S {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_E {
                if start + 3 == end { return Tk.KwUse }
            }
        }
    }
    if b0 == LOWER_W {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_H {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_E {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_N {
                    if start + 4 == end { return Tk.KwWhen }
                }
                if b3 == LOWER_R {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_E {
                        if start + 5 == end { return Tk.KwWhere }
                    }
                }
            }
            if b2 == LOWER_I {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_L {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_E {
                        if start + 5 == end { return Tk.KwWhile }
                    }
                }
            }
        }
        if b1 == LOWER_I {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_T {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_H {
                    if start + 4 == end { return Tk.KwWith }
                }
            }
        }
    }
    if b0 == LOWER_Y {
        let b1 = byte_at(src, start + 1)
        if b1 == LOWER_I {
            let b2 = byte_at(src, start + 2)
            if b2 == LOWER_E {
                let b3 = byte_at(src, start + 3)
                if b3 == LOWER_L {
                    let b4 = byte_at(src, start + 4)
                    if b4 == LOWER_D {
                        if start + 5 == end { return Tk.KwReserved }
                    }
                }
            }
        }
    }
    return Tk.Identifier
}
