# break / continue — grammar fragments

```ebnf
BreakStmt    ::= 'break'
ContinueStmt ::= 'continue'
```

Bare keywords, no operands. The production rules live in `mod.av`
(`break_lang` / `continue_lang`'s grams — the source of truth).
