// Core types: AST definitions, LLVM externs, feature registry, the L1
// node-model arena store, the byte-offset span infrastructure, and the
// id-keyed side-table infrastructure (dense/sparse).
//
// dir-as-module (g2eo.1): ast.av + llvm.av + registry.av +
// type_registry.av + runtime.av + module_graph.av + ast_introspect.av +
// arena.av + span.av + side_table.av auto-load as siblings, surface flat
// at `core.{...}`.
