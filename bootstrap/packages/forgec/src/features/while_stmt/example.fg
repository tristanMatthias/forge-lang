// Canonical example of `while` with `break` and `continue`.

fn main() {
    mut sum = 0
    mut i = 0
    while i < 10 {
        i = i + 1
        if i == 3 { continue }
        if i == 7 { break }
        sum = sum + i
    }
    sum |> string |> println
    i |> string |> println
}
