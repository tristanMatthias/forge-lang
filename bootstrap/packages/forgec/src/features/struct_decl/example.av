// Canonical example of `type` (struct) declarations and field access.

type Point = {
    x: int,
    y: int,
}

let p = Point { x: 3, y: 4 }
p.x |> string |> println
p.y |> string |> println
println(string(p.x + p.y))
