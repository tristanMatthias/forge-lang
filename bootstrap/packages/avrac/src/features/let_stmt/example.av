// Canonical example of `let` and `mut` bindings.

fn main() {
    let x = 42
    x |> string |> println

    mut y = 10
    y = y + 5
    y |> string |> println

    let name = "avra"
    println(name)
}
