// Can a closure see variables from the outer scope?
// (Non-capturing lambdas can't — this should fail or produce wrong results)
mut counter = 0
let inc = () -> {
    counter = counter + 1
    counter
}
// This tests if the lambda captures `counter` or gets a stale copy
println(string(inc()))
println(string(inc()))
println(string(counter))
