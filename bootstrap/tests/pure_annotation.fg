// Test: @pure annotation is parsed and preserved on functions.

@pure
fn add(a: int, b: int) -> int {
    a + b
}

@pure
fn double(x: int) -> int {
    x * 2
}

fn main() {
    println(string(add(3, 4)))
    println(string(double(5)))
}
