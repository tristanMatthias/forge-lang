// panic with null coalesce — success path
fn find(id: int) -> string? {
    if id == 1 { return "Alice" }
    null
}
let user = find(1) ?? panic("user not found")
println(user)

// Direct panic would exit — test only the success path
println("no panic")
