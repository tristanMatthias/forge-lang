/// expect: 2
/// expect: 4
/// expect: 6
/// expect: 4
/// expect: 5

fn main() {
    let nums = [1, 2, 3]
    let doubled = [x * 2 for x in nums]
    doubled.foreach((x) -> println(string(x)))
    
    let all = [1, 2, 3, 4, 5]
    let big = [x for x in all if x > 3]
    big.foreach((x) -> println(string(x)))
}
