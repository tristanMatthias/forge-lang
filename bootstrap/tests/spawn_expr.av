// Spawn expression: spawn { body } creates a task.

extern fn avra_thread_join(handle: int)

fn main() {
    // Basic spawn
    let h = spawn {
        println("spawned")
    }
    avra_thread_join(h)

    // Spawn with capture
    let msg = "captured"
    let h2 = spawn {
        println(msg)
    }
    avra_thread_join(h2)

    // Spawn returns a handle (int)
    let h3 = spawn {
        println("done")
    }
    avra_thread_join(h3)
}
