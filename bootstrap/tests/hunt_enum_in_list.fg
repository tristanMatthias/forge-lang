// List of enum values
enum Color { Red, Green, Blue(shade: int) }
let colors = [Color.Red, Color.Green, Color.Blue(42)]
for c in colors {
    match c {
        .Red -> println("red")
        .Green -> println("green")
        .Blue(s) -> println(`blue(${string(s)})`)
    }
}
