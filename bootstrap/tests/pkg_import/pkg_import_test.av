use @std.cli.{CommandSpec}

spec "pkg import" {
    then "@std.cli package import provides CommandSpec" {
        let c = CommandSpec { name: "app", description: "desc", hidden: false, flags: [], args: [], options: [] }
        c.name == "app"
    }
}
