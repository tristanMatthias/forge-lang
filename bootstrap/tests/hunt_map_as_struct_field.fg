// Map stored in struct field
type Config = { settings: int }
let cfg = Config { settings: 0 }
let m = {}
m.set("host", "localhost")
m.set("port", "8080")
// Can't store map in struct (it's an int field) — test map operations
println(m.get("host"))
println(string(m.length))
// Iterate keys
let keys = m.keys()
println(string(keys.length))
