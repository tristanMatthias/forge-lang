fn bsc_chain() -> string {
    "  Hello, World!  "
        .trim()
        .replace("World", "Avra")
        .upper()
}

fn bsc_join() -> string {
    let parts = "a:b:c:d".split(":")
    parts.reduce("", (acc, x) -> {
        if acc == "" { return x }
        acc + "-" + x
    })
}

spec "break string chain" {
    then "chained string methods" {
        bsc_chain() == "HELLO, AVRA!"
    }
    then "chain length" {
        bsc_chain().length == 12
    }
    then "contains check" {
        bsc_chain().contains("AVRA") == true
    }
    then "starts_with check" {
        bsc_chain().starts_with("HELLO") == true
    }
    then "index_of check" {
        bsc_chain().index_of("AVRA") == 7
    }
    then "split and rejoin" {
        bsc_join() == "a-b-c-d"
    }
}
