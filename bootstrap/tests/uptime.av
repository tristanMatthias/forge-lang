// Process uptime timing.

extern fn avra_uptime_ms() -> int

fn main() {
    let t = avra_uptime_ms()
    // Uptime should be > 0 (process has been running)
    if t >= 0 {
        println("uptime ok")
    }
}
