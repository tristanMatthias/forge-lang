extern fn avra_shell_exec(cmd: string) -> string

spec "err import not found" {
    then "F3101 reported when use imports a name that doesn't exist" {
        let out = avra_shell_exec("./build/bs2 compile tests/err_import_not_found/main.av 2>&1 || true")
        out.contains("F3101")
    }
}
