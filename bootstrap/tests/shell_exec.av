// Shell execution via C runtime.

extern fn avra_shell_exec(cmd: string) -> string

fn main() {
    let output = avra_shell_exec("echo hello")
    println(output)
    let calc = avra_shell_exec("expr 2 + 3")
    println(calc)
}
