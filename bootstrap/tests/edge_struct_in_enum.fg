type Point = { x: int, y: int }
enum Shape {
    Circle(center: Point, radius: int),
    Line(start: Point, end: Point),
}

let c = Shape.Circle(Point { x: 10, y: 20 }, 5)
match c {
    .Circle(center, r) -> println(`circle r=${string(r)}`)
    .Line(s, e) -> println("line")
}
