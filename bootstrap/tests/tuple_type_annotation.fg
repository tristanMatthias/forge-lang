/// expect: 42
/// expect: hello
/// expect: 2, 1

fn swap(a: int, b: int) -> (int, int) {
    (b, a)
}

fn main() {
    let t: (int, string) = (42, "hello")
    println(string(t.0))
    println(t.1)
    let s = swap(1, 2)
    println("${string(s.0)}, ${string(s.1)}")
}
