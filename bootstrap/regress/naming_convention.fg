// Test: naming convention warnings are emitted but don't block compilation.
// The test still runs successfully — warnings are non-fatal.

type MyStruct = { x: int }

fn good_func() -> int { 42 }

fn main() {
    let result = good_func()
    let s = MyStruct { x: result }
    println(string(s.x))
}
