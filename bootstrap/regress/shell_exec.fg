// Shell execution via C runtime.

extern fn forge_shell_exec(cmd: string) -> string

fn main() {
    let output = forge_shell_exec("echo hello")
    println(output)
    let calc = forge_shell_exec("expr 2 + 3")
    println(calc)
}
