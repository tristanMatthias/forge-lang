mod mathlib

use mathlib.{add, mul}

fn main() {
    // Exported functions accessed via use import
    println(string(add(3, 4)))
    println(string(mul(5, 6)))
    // Non-exported function accessed via global search (backward compat)
    println(string(internal_helper()))
}
