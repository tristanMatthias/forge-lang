// Process uptime timing.

extern fn forge_uptime_ms() -> int

fn main() {
    let t = forge_uptime_ms()
    // Uptime should be > 0 (process has been running)
    if t >= 0 {
        println("uptime ok")
    }
}
