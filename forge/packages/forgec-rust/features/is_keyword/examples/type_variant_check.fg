/// expect: true
/// expect: false
/// expect: true

fn divide(a: int, b: int) -> Result<int, string> {
  if b == 0 { Err("division by zero") } else { Ok(a / b) }
}

fn main() {
  println(string(divide(10, 2) is Ok))
  println(string(divide(10, 0) is Ok))
  println(string(divide(10, 0) is Err))
}
