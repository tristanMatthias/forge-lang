/// Test: string.split() returns list of substrings
///
/// expect: 3
/// expect: a
/// expect: b
/// expect: c

fn main() {
  let csv = "a,b,c"
  let parts = csv.split(",")
  println(string(parts.length))
  println(parts[0])
  println(parts[1])
  println(parts[2])
}
