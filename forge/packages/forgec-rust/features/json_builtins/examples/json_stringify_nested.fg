/// Test: json.stringify handles nested structs and lists
///
/// expect: {"name":"alice","scores":[10,20,30]}

type UserData = { name: string, scores: List<int> }

fn main() {
  let data: UserData = { name: "alice", scores: [10, 20, 30] }
  println(json.stringify(data))
}
