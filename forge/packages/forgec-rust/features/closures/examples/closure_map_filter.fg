/// Test: Closure with filter to find even numbers
///
/// expect: 2
/// expect: 4
/// expect: 6

fn main() {
  let nums = [1, 2, 3, 4, 5, 6]
  let evens = nums.filter((n) -> n % 2 == 0)
  for n in evens {
    println(string(n))
  }
}
