/// expect-check: pass
/// Verifies server middleware syntax compiles correctly

use @std.http.{server}

server :8080 {
  use_middleware "logger" (req_ptr) -> {
    0
  }

  route GET /health -> {
    "ok"
  }
}
