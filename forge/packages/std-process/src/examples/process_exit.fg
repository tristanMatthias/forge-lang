/// Demonstrate process.exit() — exits the process with the given code.
/// expect: before exit

use @std.process

fn main() {
    println("before exit")
    process.exit(0)
    println("after exit")
}
