/// # Multiline Struct Literal
///
/// Struct literals with 3+ fields on separate lines.
///
/// expect: 1 2 3

type Point = { x: int, y: int, z: int }

fn main() {
  let p = Point {
    x: 1,
    y: 2,
    z: 3
  }
  println(`${p.x} ${p.y} ${p.z}`)
}
