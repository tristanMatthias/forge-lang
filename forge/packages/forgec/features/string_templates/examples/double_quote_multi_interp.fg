/// # Double-Quoted Multiple Interpolations
///
/// Multiple `${...}` expressions in a single double-quoted string.
///
/// expect: a 1 c 2

fn main() {
  let b = 1
  let d = 2
  println("a ${b} c ${d}")
}
