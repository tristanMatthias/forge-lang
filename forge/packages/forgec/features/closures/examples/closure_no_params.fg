/// # Closure with Single Implicit Param
///
/// Closures using the `it` shorthand for single-parameter usage.
///
/// expect: 2
/// expect: 6
/// expect: 10

fn main() {
  let nums = [1, 3, 5]
  let doubled = nums.map(it * 2)
  for n in doubled {
    println(string(n))
  }
}
