// ─── Variables Feature ─────────────────────────
// Handles: let/mut bindings
// Parse handler + codegen handler, registered in registry

fn register_variables() {
    register_parse("let", parse_let_handler)
    register_parse("mut", parse_let_handler)
    register_codegen("Let", codegen_let)
}
