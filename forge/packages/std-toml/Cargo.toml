[package]
name = "forge-toml"
version = "0.1.0"
edition = "2021"

[lib]
name = "forge_toml"
crate-type = ["staticlib", "cdylib"]

[dependencies]
toml = "0.8"
serde_json = "1"
serde = { version = "1", features = ["derive"] }
