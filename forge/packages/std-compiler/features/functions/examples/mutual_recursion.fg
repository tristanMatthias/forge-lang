/// # Mutually Recursive Functions
///
/// is_even and is_odd call each other recursively.
///
/// expect: true
/// expect: false
/// expect: true
/// expect: false

fn is_even(n: int) -> bool {
  if n == 0 {
    return true
  }
  return is_odd(n - 1)
}

fn is_odd(n: int) -> bool {
  if n == 0 {
    return false
  }
  return is_even(n - 1)
}

fn main() {
  println(string(is_even(4)))
  println(string(is_even(3)))
  println(string(is_odd(5)))
  println(string(is_odd(6)))
}
