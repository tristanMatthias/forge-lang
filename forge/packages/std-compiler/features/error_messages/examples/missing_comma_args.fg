/// # Extra argument to built-in
///
/// Calling a two-param function with three args.
///
/// expect-error: F0014

fn add(a: int, b: int) -> int {
    return a + b
}

fn main() {
    let x = add(1, 2, 3, 4)
}
