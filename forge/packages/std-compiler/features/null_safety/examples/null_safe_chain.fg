/// Test: Null-safe member access with ?.
///
/// expect: 5
/// expect: fallback

fn get_name() -> string? { "hello" }
fn get_nothing() -> string? { null }

fn main() {
  let a = get_name()
  println(string(a?.length ?? 0))

  let b = get_nothing()
  println(b ?? "fallback")
}
