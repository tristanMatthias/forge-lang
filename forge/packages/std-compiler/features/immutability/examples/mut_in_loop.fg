/// # Mutable Variable in Loop
///
/// A mut variable accumulated across loop iterations.
///
/// expect: 100

fn main() {
  mut sum = 0
  mut i = 0
  while i < 100 {
    sum = sum + 1
    i = i + 1
  }
  println(string(sum))
}
