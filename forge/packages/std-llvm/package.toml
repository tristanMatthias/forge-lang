[package]
name = "llvm"
namespace = "std"
version = "0.1.0"
description = "LLVM C API wrapper for generating LLVM IR from Forge"

[native]
library = "forge_llvm"
link_flags = [
    "-L/opt/homebrew/opt/llvm@19/lib",
    "-lLLVM-19",
    "-lc++",
]
