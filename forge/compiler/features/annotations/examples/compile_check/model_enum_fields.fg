/// expect-check: pass
use @std.model.{model}

enum Status { draft, published, archived }

model Post {
    id: int @primary @auto_increment
    title: string
    status: string @default("draft")
}

fn main() {
    let s = Status.draft
    println("enum fields compile")
}
