// Parallel execution parsing.
// Currently uses the same `spawn` keyword — parallel blocks are a future extension
// that will add structured concurrency primitives.
// The SpawnBlock AST node handles both spawn and parallel cases.
