/// Test: Closure with list.map for squaring numbers
///
/// expect: 1
/// expect: 4
/// expect: 9

fn main() {
  let nums = [1, 2, 3]
  let squared = nums.map((n) -> n * n)
  for n in squared {
    println(string(n))
  }
}
