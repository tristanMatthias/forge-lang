// The `it` parameter is handled entirely at parse time: the parser wraps `it`-containing
// expressions in closures before type inference ever sees them. No additional type
// inference logic is needed here.
