/// # Nested Function Calls
///
/// Functions called as arguments to other functions.
///
/// expect: 30

fn double(x: int) -> int {
  return x * 2
}

fn add(a: int, b: int) -> int {
  return a + b
}

fn main() {
  println(string(add(double(5), double(10))))
}
