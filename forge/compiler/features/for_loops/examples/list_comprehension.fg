/// Test: for loop with list building via map and filter
///
/// expect: 2, 4, 6, 8, 10

fn main() {
  let nums = [1, 2, 3, 4, 5]
  let doubled = nums.map((n) -> n * 2)
  println(doubled.map((n) -> string(n)).join(", "))
}
