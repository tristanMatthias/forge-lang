/// # List Iteration
///
/// Iterate over a list with `for ... in`.
///
/// expect: 6

fn main() {
  let items = [1, 2, 3]
  mut total = 0
  for item in items {
    total = total + item
  }
  println(total)
}
