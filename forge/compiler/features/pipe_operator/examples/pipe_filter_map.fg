/// Test: Pipe operator chaining filter and map
///
/// expect: 4
/// expect: 16
/// expect: 36

fn main() {
  let nums = [1, 2, 3, 4, 5, 6]
  let result = nums
    |> filter((n) -> n % 2 == 0)
    |> map((n) -> n * n)
  for n in result {
    println(string(n))
  }
}
