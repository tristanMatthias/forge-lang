/// # Function missing return type arrow
///
/// Return types need `->` before the type name.
///
/// expect-error: F0001

fn add(a: int, b: int) int {
    return a + b
}

fn main() {
    print(add(1, 2))
}
