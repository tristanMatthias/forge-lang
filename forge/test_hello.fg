fn count_to(n: int) -> int {
    mut count = 0
    mut i = 0
    while i < n {
        count = count + 1
        i = i + 1
    }
    return count
}

fn main() {
    let result = count_to(5)
    println(result)
    println("done!")
}
