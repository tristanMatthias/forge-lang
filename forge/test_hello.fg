fn is_keyword(text: string) -> int {
    if text == "fn" { return 1 }
    if text == "let" { return 1 }
    if text == "if" { return 1 }
    return 0
}

fn main() {
    println(is_keyword("fn"))
    println(is_keyword("hello"))
    println(is_keyword("let"))
    println("done!")
}
