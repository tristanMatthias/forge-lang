fn count_chars(s: string) -> int {
    mut count = 0
    mut i = 0
    while i < s.length {
        count = count + 1
        i = i + 1
    }
    return count
}

fn main() {
    let n = count_chars("hello")
    println(n)
    println("done!")
}
