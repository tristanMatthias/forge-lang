fn is_keyword(text: string) -> int {
    if text == "fn" { return 1 }
    if text == "let" { return 1 }
    return 0
}

fn fib(n: int) -> int {
    if n <= 1 { return n }
    return fib(n - 1) + fib(n - 2)
}

fn main() {
    println(fib(15))
    println(is_keyword("fn"))
    println("done!")
}
