use @std.process

fn main() {
  let result_json = process.run("echo", ["hello forge"])
  let result: {stdout: string, stderr: string, code: int} = json.parse(result_json)
  println(result.stdout.trim())
  println(string(result.code))
}
