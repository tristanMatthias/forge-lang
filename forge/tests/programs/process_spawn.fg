use @std.process

fn main() {
  let handle = process.spawn("sleep", ["30"])
  println(string(handle > 0))

  let alive = process.is_alive(handle)
  println(string(alive))

  process.kill(handle)
  sleep(50)

  let dead = process.is_alive(handle)
  println(string(dead))
}
